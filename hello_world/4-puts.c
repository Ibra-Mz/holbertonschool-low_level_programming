#include <stdio.h>
/**
* Main function return 0
* followed by new line
* used fun is puts
* ends with 0
*/
int main(void)
{
puts("\"Programming is like building a multilingual puzzle");
return (0);
}
