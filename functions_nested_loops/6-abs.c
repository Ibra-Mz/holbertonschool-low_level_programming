#include "main.h"

/**
 * _abs - function that computes the absolute value of an integer
 * @n: the number to check
 *
 * Return: -n if negative, n if else
 */
int _abs(int n)
{
if (n < 0)
return (-n);
else
return (n);
}
